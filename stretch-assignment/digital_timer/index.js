let inetervalID = 0
const digits = document.querySelectorAll(".digit")

function startTimer() {
   intervalID = window.setInterval(timer, 10)
}

function stopTimer() {
    clearInterval(inetervalID)
}

function timer() {
    
}